export const regexNumber = /^\(?\d{3}\)?[-.\s]?\d{3}[-.\s]?\d{4}$/;
export const regexSapce = /^(?!\s*$).+/;
export const regexDate = /^\d{4}$/;
export const regexCaps = /^[a-zA-Z]+$/;
export const regexNumOnly = /^\d{12,}$/;